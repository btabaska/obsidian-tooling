{
  "$comment": "Kanban -> intentionally left at plugin defaults. Kanban stores per-board configuration in each board note's frontmatter, so there is little useful global config to pre-bake. This file is effectively empty; bootstrap strips this comment and writes {}."
}
